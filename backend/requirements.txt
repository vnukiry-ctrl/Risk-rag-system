# Pinned from the working dev venv (pip freeze), trimmed to what main.py's
# own import chain actually needs -- not a full freeze, which would drag in
# ~80 transitive/experimentation packages (langgraph, langchain-community,
# etc.) never imported by the app itself. See docs/adr/ for why each
# swappable-provider package (anthropic, langchain-anthropic) is here even
# though the default config doesn't use it -- ADR-provider-swap feature.
fastapi==0.141.1
uvicorn==0.52.3
python-dotenv==1.2.3
pydantic==2.13.4
pypdf==6.16.1
pymupdf==1.28.2
qdrant-client==1.19.0
langchain-text-splitters==1.1.2
requests==2.34.2
openai==3.2.0
slowapi==0.1.10
python-docx==1.2.0
pytesseract==0.3.13
pillow==12.3.0
anthropic==0.122.0
langchain-anthropic==1.5.6
pytest==9.1.1
